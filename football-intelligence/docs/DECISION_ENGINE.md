# Decision engine

`QUALIFIED` requires all gates: complete and fresh data, resolved critical intelligence, adequate lineup certainty, strong agreement, sufficient reliability, bounded uncertainty and positive expected value at the manually entered price.

`WATCH` means the current information is incomplete but a scheduled/expected update could change the evaluation. `NO BET` is the normal outcome when a gate fails or the offered price is not good enough. Outcome probability is never a reliability score.

